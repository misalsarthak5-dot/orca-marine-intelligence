'use client';
import PlaceholderPage from '@/components/PlaceholderPage';
import { AlertTriangle } from 'lucide-react';
export default function AlertsPage() {
  return <PlaceholderPage title="Marine Alerts" description="Comprehensive alert history, active warnings, cyclone tracking, lightning risk monitoring, and marine advisory management." icon={AlertTriangle} />;
}
