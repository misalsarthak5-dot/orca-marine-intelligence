'use client';
import PlaceholderPage from '@/components/PlaceholderPage';
import { MessageCircle } from 'lucide-react';
export default function AskPage() {
  return <PlaceholderPage title="Ask ORCA" description="Full conversational marine decision support interface with multi-turn context, multilingual interaction, and explainable AI responses." icon={MessageCircle} />;
}
