'use client';
import PlaceholderPage from '@/components/PlaceholderPage';
import { Settings } from 'lucide-react';
export default function SettingsPage() {
  return <PlaceholderPage title="Settings" description="Configure ORCA preferences, notification settings, display options, language defaults, data sources, and API integrations." icon={Settings} />;
}
