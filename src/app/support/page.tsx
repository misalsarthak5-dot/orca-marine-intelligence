'use client';
import PlaceholderPage from '@/components/PlaceholderPage';
import { HelpCircle } from 'lucide-react';
export default function SupportPage() {
  return <PlaceholderPage title="Support" description="Documentation, FAQs, contact information, and system diagnostics for the ORCA Marine Intelligence Platform." icon={HelpCircle} />;
}
